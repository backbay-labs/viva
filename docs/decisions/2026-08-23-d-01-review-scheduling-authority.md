# D-01 SCHEDULING_AUTHORITY_EXAM — Recorded Decision

- Program decision ID: `D-01 SCHEDULING_AUTHORITY_EXAM`
- Decided by: Connor (coordinator-recommended fast-path set, approved under the 2026-08-24 execution directive; amendable by Connor before dependent GREEN work consumes it)
- Date: 2026-08-24
- Registry recording: the ledger's `D-01` Current state cell is replaced in place with `SERVER_PERSISTED_FSRS` in the same commit as this document.

Selected authority: `SERVER_PERSISTED_FSRS`

FSRS policy version: FSRS-6 default parameter set (the 21-parameter default weights shipped as the ts-fsrs 5.4.1 `generatorParameters()` defaults), with default request retention 0.9 and no fuzzing; `policy_id` is exactly `viva.fsrs6-default.1`. The conformance fixture literals freeze this policy; if ts-fsrs 5.4.1 defaults, the reference oracle, and the pinned Rust crate cannot agree byte-for-semantic-byte, execution stops and this document is amended — workers never tune fixture literals to an implementation.
Status-to-rating mapping: `missed` -> 1 (Again), `review` -> 2 (Hard), `shaky` -> 3 (Good), `strong` -> 4 (Easy). Unknown hint/miss provenance remains `None`/`null` and is never converted to zero; hints and misses do not modify the rating, only the recorded provenance.
UTC rule: every scheduling computation and persisted timestamp is an exact UTC instant serialized as RFC3339 with `Z` and millisecond precision; there is no calendar-day rounding, no timezone-local arithmetic, and no DST handling anywhere server-side. Exam timestamps are compared as stored UTC instants. Learner-facing calendar labels are derived client-side on the runtime-local calendar (Plan 10 Task 10) and are display-only.
Exam margin: 86400 seconds (24 hours). When the study set has a future `exam_at` (`exam_at > graded_at`), the scheduled `due_at` is `min(uncapped_due_at, exam_at - 86400s)` with `cap_reason = exam_margin` recorded if and only if the cap strictly lowered `uncapped_due_at`. No scheduled `due_at` may ever be later than `exam_at`.
Past-exam rule: when `exam_at <= graded_at` (the exam is already past at grading time), the decision is persisted fail-closed with `due_at = exam_at` and `cap_reason = past_exam`; no future review is ever created for that study set's concepts. The authenticated read model excludes `past_exam`-capped decisions from the learner-visible `reviewSchedule` and reports the concept's `dueAt` as `null`.
Schema owner: `agent-domain` owns the `PersistedFsrsCardV1` and `ReviewScheduleDecisionV1` v1 type definitions (`schema_version` exactly 1); Plan 09 is the sole migration-number allocator and sole owner of `agent/migrations/**` after the Plan 03 first merge. Plan 03 creates only `agent/migrations/0015_review_schedule_decisions_v1.sql`.
Rollback: the v1 schema is additive-only with no destructive down-migration. Deployment rollback re-reads valid v1 rows unchanged. Migration 0015 marks the four known buggy fixed-date rows (`2026-06-18/19/20/24T09:00:00Z`) as `superseded` without inventing replacement dates; the authenticated library query selects only valid v1 decisions and never falls back to a superseded row.
Reference oracle: official open-spaced-repetition Python FSRS (`py-fsrs`, PyPI package `fsrs`) release 6.3.2, executed once in a disposable `uv` virtual environment to derive the fixture literals; artifact digests are PyPI sha256 `bc55066c821164b1cff6d75b17a3ed8e86cfd45789e3d775cfcb895319ce9ba4` (`fsrs-6.3.2-py3-none-any.whl`) and `7fe79947ceb92ca35fdca3dfa511a239be15b317cde63d6643a2da6110844d91` (`fsrs-6.3.2.tar.gz`). The oracle is independent of both production paths (ts-fsrs and the Rust crate); a second reviewer manually checks every literal against the published FSRS-6 equations per Plan 03 Task 7 Step 5.
Rust FSRS crate: `fsrs` (open-spaced-repetition/fsrs-rs) version 6.6.1, pinned exactly in `agent/crates/agent-domain/Cargo.toml` and `agent/Cargo.lock`. If this crate cannot reproduce the independent oracle fixture exactly, Plan 03 Task 8A Step 5 stops and this document is amended; expected literals are never tuned to the implementation.

## Rejected branch

`EVENTS_PLUS_READ_TIME_PROJECTION` (Branch B) was considered and rejected for this cycle. It would persist only immutable `ReviewHistoryEventV1` rows (no due date anywhere in Rust) and compute `ReadTimeReviewProjectionV1` exclusively in `packages/core` while building the authenticated read model, with Plan 11 attaching the projection at the BFF. Rejection rationale: Branch A yields one authoritative persisted schedule with simpler reads and fewer conditional seams (Branch B additionally requires temporary Plan 03 read seams in `agent-service/src/app.rs`, the BFF library route, and `viva-library.ts`), at the acceptable cost of pinning one Rust FSRS implementation that is differentially checked against the shared literal fixture. No Branch B artifact may be created: `agent/migrations/0015_review_history_events_v1.sql` and `agent/crates/agent-domain/src/review_history.rs` must not exist (Plan 03 Task 8A Step 11 proves their absence).
