# Coordinator-Ratified Plan Amendments

Recorded by the integration coordinator. Each amendment supersedes the quoted plan text for every consuming lane; lanes pick these up on rebase and via coordinator handoff notes. Workers never author amendments — they escalate, the coordinator ratifies.

## A-01 (2026-08-24) — Plan 03 Task 8A Step 3: `persist_review_schedule_decision` port signature

**Amended (ratified) signature** — replaces the five-parameter form locked in Plan 03 Task 8A Step 3:

```rust
async fn persist_review_schedule_decision(
    &self,
    user_id: &str,
    study_set_id: &str,
    voice_session_id: &str,
    response_id: &str,
    concept_id: &str,
    decision: ReviewScheduleDecisionV1,
) -> Result<Value, PortError>;
```

**Rationale:** the locked five-parameter signature carried no idempotency key, while Plan 03 Task 8A Step 2 requires "replay of the same authorized response returns the exact stored decision" and Plan 04 LEARN-003A fixes "the authorized response identity is the existing idempotency source". `response_id` (inserted before `concept_id`) is the response-keyed idempotency identity. `ReviewScheduleDecisionV1` itself is unchanged. Escalated by the Plan 03 worker (amendment request preserved in lane evidence), independently verified by the branch reviewer on real PostgreSQL 16, ratified under Connor's 2026-08-24 execution directive.

**Downstream effect:** Plans 04 (LEARN-003A consumption), 06 (`ports.rs` preservation/export), and 09 (memory/Postgres overrides and conformance tests) consume, preserve, and implement the six-parameter form. Any plan text quoting the five-parameter form reads as amended.

## A-02 (2026-08-24) — Plan 03 Task 8A: migration `0015_review_schedule_decisions_v1.sql` additionally adds `study_sets.exam_at`

**Amended content:** `0015` also runs `ALTER TABLE study_sets ADD COLUMN IF NOT EXISTS exam_at TIMESTAMPTZ` (nullable, additive, no destructive down-migration).

**Rationale:** D-01's recorded UTC rule ("exact UTC instants, no calendar-day rounding") cannot be represented by the legacy `0001` `DATE` exam column, and Plan 03 may create only migration `0015` (Plan 09 is sole allocator afterward), so the column rides in `0015`.

**Downstream obligations:**
- Every writer of `study_sets` rows must populate `exam_at` (from the ingestion `exam_date` input interpreted per the D-01 UTC rule) or the D-01 exam cap has no input for that row — binds Plan 08 (ingestion handlers) and Plan 09 (stores/seed/backfill semantics).
- Plan 09 must not re-add or duplicate this column in `0016`–`0018` and treats it as part of the selected D-01 v1 seam.

## Coordinator notes attached to the same review pass (not amendments)

1. **Extended parallel-flake list** (baseline, full-workspace parallel load only; pass in isolation): in addition to `websocket_turn_cap_ignores_suppressed_stale_resolution_after_new_submission`, also `websocket_turn_cap_ignores_response_less_phase_after_new_submission` and `websocket_turn_cap_is_not_postponed_by_client_keepalives`. The 4 deterministic LANE_BASE reds are unchanged. Plan 08 owns the underlying timing sensitivity.
2. **Advisory-lock pool note:** `pg_advisory_xact_lock` in the schedule-decision write holds a pooled connection for the transaction duration; racers can occupy pool slots (pool=5). No deadlock. A bounded lock-wait timeout is a Plan 04/09 follow-up consideration when they take over scheduling behavior — recorded in the Plan 04 handoff.

## A-03 (2026-08-24) — Node 12A additionally carries the script protocol-constant bump

Plan 03's locked v5 bump necessarily breaks two baseline script tests that assert the Plan-12-owned scripts share the voice protocol version: `scripts/e2e-browser.mjs:65` (`VIVA_VOICE_PROTOCOL_VERSION = 4`) and `scripts/live-provider-smoke.mjs:13` (`PROTOCOL_VERSION = 4`). **Ratified:** integration node 12A additionally includes one commit changing exactly those two literals to `5`. The bump is only green on a post-Plan-03 tree, so it is authored on the 12A admission branch after the Plan 03 merge and admitted immediately after it (Plan 03 merge → 12A, no intervening lane merge; the 13A-lockfile constraint then applies to a later 12A-suffix commit as already recorded). The content is fully determined by this amendment; the coordinator applies it mechanically citing A-03, with Plan 12 retaining ownership (its Task 13/RELEASE-028 later replaces the literals with strict validator consumption). Latent-defect note from the Plan 03 escalation stands: `e2e-browser.mjs`'s failure-control replay-nonce path would have sent a v4 frame at a v5 server if the harness were enabled; the default release run never reaches it. Plan 12's Task 13 closes it structurally.

## W-01 (2026-08-24) — Merge-gate waiver: recorded baseline voice_ws failures do not block the Plan 03 merge

Plan 03 Task 9 Step 5 requires `bun run validate` green on the PR head. **Waived, exactly and only, for the seven coordinator-recorded baseline `voice_ws` names** (4 deterministic: audio_continuation_second_lease, provider_drains_queued_usage, queue_cancel_drops_pending_admission, queue_cancel_rearms_pre_answer_idle; 3 load-flaky: turn_cap_suppressed_stale_resolution, turn_cap_response_less_phase, turn_cap_not_postponed_by_keepalives). Basis: these are pre-existing at the audit baseline (hosted Validate red on `4d5d8276`), Plan 03's T9 evidence proves every failing Rust test observed is a strict subset of these names with zero new reds, and the failing area is Plan 08's permanent ownership (provider-queue/lease/turn-cap). Disposition owner: Plan 08's lane, whose SERVICE-001/002/006/008 work rewrites these paths; Plan 15's Level 2 gate still requires a fully green workspace on the frozen combined SHA, so this waiver expires there and cannot leak into release evidence. Any failure outside these seven names blocks the merge.

## A-04 (2026-08-24) — Node 12A admitted via sub-branch `review-remediation/12a`

Node 12A's lane branch (`review-remediation/12-release-ci`) had already advanced past the 12A boundary with baseline-safe Task 1–4 commits, so the 12A PR (#100) was cut from sub-branch `review-remediation/12a` carrying exactly the two 12A manifest/lock commits plus the A-03 constant bump, rebased onto the post-Plan-03 integration tip. Plan 15's expected-branch verification for node 12A reads as amended to accept `review-remediation/12a` (same lane worktree source; the program's split-node rule "commits/PRs from the same lane worktree, not extra owners or permanent branches" is preserved — the sub-branch is retired after Plan 15's capture). Lane 12 rebases over the duplicate commits at its 12B rebase.

## A-05 (2026-08-24) — Lanes commit deterministic lockfile deltas of plan-sanctioned dependency edits

Ratified at the node 06 merge (first recorded in the coordinator execution log; transcribed here for the authoritative record). When a plan sanctions a manifest/dependency edit (e.g. Plan 06's trybuild/proptest dev-dependencies), the lane commits the deterministic `Cargo.lock`/`bun.lock` delta **in the same commit** as the manifest edit, even where the plan's quoted `git add` list omits the lockfile. Plan 12 retains lockfile audit authority and reconciles all lock history at its 12B rebase. Basis: an integration tip whose manifests and lock disagree fails every `--frozen-lockfile`/`--locked` gate and poisons downstream lanes; the omission in plan text was mechanical, not a design choice. First application: node 06's `agent/Cargo.lock` (+221 lines), committed by the coordinator before merge.

## W-02 (2026-08-24) — Pre-authorized merge-gate waiver: labeled `quiz_only_` reds at the node 04B merge

Node 04B commits RED tests labeled `quiz_only_` whose GREEN is produced by Plan 06's Task 1A (D-03B `brain.rs` quiz-affordance removal), which merges immediately after 04B in lane 06's second PR. **Pre-authorized:** the 04B merge gate accepts exactly the labeled `quiz_only_` failures and nothing else; the waiver expires the moment 06 Task 1A merges, and Plan 15's Level 2 full-green gate is unaffected. Any red outside the labeled set blocks the 04B merge normally.

## A-06 (2026-08-25) — Plan 12 Step 4A3's lockfile-regeneration commit rides inside the node 13A admission

Plan 12 constraint 9(c)/Step 4A3 requires the Bun 1.3.3 `bun.lock` regeneration for Plan 13's Task 1/2 manifest commits to merge "immediately before 13A with no intervening lane merge". A lock regenerated on lane 12's tree cannot see Plan 13's manifests, so the constraint is unsatisfiable as literally written. **Ratified:** the regeneration commit is authored by the coordinator on the 13A admission branch (`review-remediation/13a`), directly on top of lane 13's head, with Bun 1.3.3 via `mise x bun@1.3.3`, and merges inside the 13A PR (#103, commit `6bffdf0`). The material invariant is preserved strictly: no integration tip ever carries the manifests without the matching lock, and `bun install --frozen-lockfile` is green on the admitted head. Plan 12 retains `bun.lock` ownership; lane 12 rebases over it at 12B. Observed delta: workspace-metadata only (ui-web `react` → peer/dev, `@viva/tokens` workspace dep).

## A-07 (2026-08-25) — Mechanical completion of a Plan-10-owned test fixture broken by Plan 03's type change

Plan 03's `ReviewScheduleItem` gained `authoritativeDueAt`/`capReason`/`card`; the pre-existing fixture in `apps/web/components/session/LiveSessionShell.test.tsx` (Plan-10-owned, outside Plan 03's temporary ownership — which is exactly why no lane updated it) left the integration tip's `apps/web` typecheck red with one error after the Plan 03 merge. **Ratified:** one coordinator-applied mechanical commit (`d1331c9`, inside the 13A admission) adds the three missing fields to the fixture literal with type-correct values consistent with its existing dates; no production code or assertion changes. Plan 10 retains ownership of the file and may freely rewrite the fixture at node 10. Basis: same category as A-03 (deterministic cross-lane fallout whose content is fully determined by the merged type).

## A-08 (2026-08-25) — Mechanical alignment of Plan-10-owned web surfaces broken by D-01A

LEARN-003A's locked step deleted `dueDateForStatus`/`reviewIntervalForStatus` from `packages/core` (client-side FSRS fabrication — the D-01 defect itself), leaving two Plan-10-owned surfaces stale: the dangling `conceptStatusVerdict` call site in `apps/web/lib/viva-session-projection.ts`, and a `viva-display.test.ts` assertion demanding the fabricated "session recency cap" claim that truthful cap explanations now correctly suppress. **Ratified:** two coordinator-applied commits inside the 04B admission (PR #104): the verdict renders the honest status label only, and the assertion now asserts the binding-cap-only semantics (including a negative assertion on the fabricated claim). A behavior-preserving shim was rejected because it would resurrect the outlawed client-side fabrication. Plan 10 owns both files wholesale at node 10 and builds the authoritative in-session review verdict there. Basis: A-03/A-07 category — deterministic cross-lane fallout; content determined by the recorded decision (D-01A truthfulness), not coordinator invention.

## W-04 (2026-08-25) — Known-red window: one TS5097 in `apps/web` typecheck between nodes 04B and 14A

Plan 04's locked TS steps mandate pure-ESM `.ts`-suffix imports in the new `packages/core` modules; the enabling `"allowImportingTsExtensions": true` in `tsconfig.base.json` is Plan 14's Task 2 Step 1–2 obligation (Phase 14A). **Recorded:** from the 04B merge until the 14A merge, `apps/web` typecheck carries exactly one error — TS5097 on `packages/core/src/learner-loop-contract.ts` — and merge gates treat exactly that error as the recorded known-red. Any other typecheck error blocks normally. Node 14A merges immediately after 04B, so the window is one node wide.


## A-09 (2026-08-25) — Plan 14 Task 3 Step 5 / Task 7 Step 4: bundle-isolation verification excludes source maps

The literal verification line `! rg -F "Try again using the phrase 'shuttle system'." apps/web/.next` reproducibly fails on every real `next build`: Turbopack embeds `packages/core/src/index.ts`'s full original text into server-only `*.js.map` `sourcesContent` whenever a server chunk imports anything from the file, and both the Phase-14A surface (index.ts keeps its full export surface, by plan design) and the plan's own Phase-14B target structure (`study-set.ts` keeps `createStudySetPreview` and `evaluateAnswer` co-located) put the needle in that original text permanently. The string appears in no static asset, no executable server JS, and no `sources[]`/`mappings` entry — only in debug-metadata `sourcesContent` (lane evidence task-3-step5-line4-verification.txt). **Ratified:** both occurrences of the literal line (Task 3 Step 5 fourth expectation, Task 7 Step 4) read as amended to `! rg -F "Try again using the phrase 'shuttle system'." apps/web/.next --glob '!*.map'`. The PACKAGE-02 completion criterion is unchanged and remains enforced by the committed isolation test's negative-control bundle proof (mutation-verified to detect an injected leak). No `next.config.ts` change is authorized by this amendment.

## A-10 (2026-08-25) — Plan 06 second PR: authorized Plan-04-file edits at the StudySessionRecap root swap, and related file-list exceedances

Plan 06's ownership boundary ("never modify study/tools/tool_executor.rs") is crossed by exactly two edits at the Task 0 Step 3 crate-root recap swap, both **ratified**: (1) the `record_recap` call site in `tool_executor.rs` persists the v2 recap directly — compile-forced (E0599 otherwise); (2) `study.rs` loses `StudySessionRecap::from_evidence_recap` — NOT compile-forced (verified by the branch reviewer: restoring it compiles clean under `-D warnings`), but it is the cleanup Plan 04's own recorded design built to happen at this swap (its doc comment named the swap the intended forcing function and the function a recorded LEARN-011-window cleanup), and no caller outside its doc and test remained. The in-tree shim doc originally overstated both edits as forced; corrected in the same admission. Also ratified under this amendment: Task 1A Step 2B's staging list reads as amended to include `tests/store_contract.rs` (compile-forced: its fixture named the removed `initial_goal`), and the unplanned public export `pub use study::StudySessionRecap as StudySessionRecapV1` is accepted as a labeled MIGRATION SHIM with a gate-enforced removal trigger — Plans 07/08/09 must retire it, and the retirement is added to their dispatch obligations.

## A-11 (2026-08-25) — Merged PROVENANCE.md rewording out of legacy-residue vocabulary

`apps/web/app/fonts/PROVENANCE.md:33` ("nothing in this recipe is committed…") trips `scripts/check-legacy-domain-residue.sh`'s correct `recipe` pattern, keeping `agent:residue` red on the tip since the 13A merge. **Ratified:** one coordinator commit rewords the line to "preparation procedure" (meaning preserved); the gate is not weakened. Plan 13 retains file ownership.

## Coordinator sequencing notes attached to the Plan 06 second PR (not amendments)

1. **Task 7 policy-test commit re-sequenced**: `scripts/rust-domain-quality-policy.test.mjs` asserts Plan 12B's `agent:deps:unused`/`agent:domain:mutants` commands and is red until they exist; Plan 06 Task 7 Step 5 itself gates that commit to "the same integration wave as 12b (or later)". The coordinator's earlier dispatch pulled it forward in error; the commit stays on lane 06's branch and merges at the 12B wave. The node-06-second-PR admission excludes it.
2. **Task 8 pattern defect (pre-recorded for its future dispatch)**: Task 8 Step 1's CONFIRM_DELETE absence grep includes `chrono\.workspace`, which now legitimately matches agent-domain's Cargo.toml because Plan 03's D-01 seam declares chrono. When Task 8 dispatches, the pattern reads as amended to drop `chrono\.workspace` (keeping `uuid\.workspace`).

## A-12 (2026-08-25) — Node 05 admission: redaction sanction rows, audit maxBuffer, export fence, and consumer-fixture edits

Four ratifications attached to the node 05 merge:
1. **Sanction rows (Plan-12 file, coordinator-applied):** `scripts/redaction-control.mjs` gains SOURCE_AUDIT_SAFE_MARKER_OCCURRENCES rows for `agent/crates/agent-service/src/protocol.rs` and `packages/core/src/agent-contract.ts` — 77 anchored literal patterns, one per reviewed line carrying a plan-pinned v5 wire field name (pcm16_base64, source_context, answer_text, session_token, transcript_final, prompt). The mechanism mirrors the existing ws.rs/config.rs rows; any NEW marker occurrence in these files still fails the audit. Plan 05's lane correctly refused to edit Plan 12's file or obfuscate field access.
2. **Audit defect fix (Plan-12 file):** `scripts/redaction-control-check.mjs` gains maxBuffer on its git-diff call — Node's 1 MiB default truncated large lane diffs into a hard "Unable to compute redaction diff" error.
3. **Export fence:** `agent-service/src/lib.rs`'s `pub use protocol::{…}` extension (negotiation fn, advertisement, diagnostics, supported-versions) is ratified — `mod protocol` is private and VOICE-VERSION-001's "Rust exposes" requirement forces the fence; Plan 08 preserves it.
4. **Consumer-fixture edits:** two Plan-10-owned test files (`viva-session-projection.test.ts`, `LiveSessionShell.test.tsx`) each gained exactly two lines (advertisement import + `protocol:` field in the local ready() helper) — compile-forced by the v5 ready contract, A-08 precedent; Plan 10 owns both wholesale at node 10. Making `protocol` optional was rejected as contract weakening.

## W-05 (2026-08-25) — Known-red window: Plan-10-owned web client vs the v5 contract, between nodes 05 and 10

Node 05's tightened shared contract (v2 recap shape, quiz-only mode, VivaErrorFrame without `message`, v5 client-frame union with `turn_intent` replacing `"text"`, StudyQuestion `concept_id`/`rubric`) breaks the not-yet-migrated Plan-10 web client — the designed web-side twin of the Rust consumer-compile window (adapters/service/data vs Plan 06). The live send-path migration is exactly Plan 10's node-10 scope (the DAG orders 10 after 05 for this reason); a coordinator bridge would do Plan 10's work ad hoc without its RED/GREEN discipline and is rejected.

**Recorded window, from the node 05 merge until the node 10 merge:**
- `apps/web` typecheck: exactly 20 errors in exactly three files — `lib/use-viva-agent-session.ts` (10), `lib/viva-agent-client.ts` (5), `lib/viva-agent-client.test.ts` (5).
- `apps/web` test suite: exactly 19 failures confined to four suites — Viva agent browser client, useVivaAgentSession adapter, Viva display state, bounded audio turn ledger (exact names in the coordinator log).
- `apps/web` production build is red for the same type errors.

Any typecheck error, test failure, or build failure outside this enumeration blocks normally. Intermediate merge gates (07/09/08/11) scope their web checks accordingly; node 11's own files must still typecheck clean. Expires at node 10, which must restore full web green.

## A-13 (2026-08-25) — Merge-order edge 09→07; Plan 07 Task 1 file-list extension; SyntheticFixtureAnswerEvaluator placement

1. **DAG edge added:** node 09 merges before node 07's Task 1 runs. The program DAG modeled source-level dependencies only and omitted `agent-adapters`' dev-dependency on `agent/crates/data` — every `cargo test -p agent-adapters` target links `data`, which cannot compile until Plan 09's Tasks 4/6 adopt the merged Plan 04/06 APIs. No cycle: Plan 09's gates are `-p data`/`-p agent-domain` only. (Worker-diagnosed at dispatch; lane 07 made no commits and re-dispatches after node 09.)
2. **Plan 07 Task 1 staging list reads as amended** to include `stt.rs` and `tts.rs`: their 38 compile errors are purely compile-forced (Plan 06 collapsed BrainError to the single typed Failure variant), and Task 1 cannot produce a compiling commit without minimal typed-failure edits there. Tasks 3/6/7 retain their behavioral scope in those files.
3. **SyntheticFixtureAnswerEvaluator** (named by Plan 07's seam text, absent from agent-domain): ruled a Plan-07-owned type — implemented inside Plan 07's fake/synthetic builders, implementing Plan 04's published `AnswerEvaluator` trait, deriving decisions only from the Plan 04 learning-core fixtures, constructible only through named fake/synthetic builders (never default or environment-controlled). Plan 04 publishes the trait, not the fixture evaluator.

## A-14 (2026-08-25) — SessionLearningEvidence gains two session-scoped question fields; supplementary 04B-fix admission; Plan 09 population obligation

The independent branch review found (and the Plan 04 lane's verify-then-fix reproduced with a RED two-turn executor test) that `evaluate_spoken_answer` gated on the study-set-global, session-blind `StudyMemoryStore::active_question`, rejecting the second answer of every session. **Ratified, all four parts:**

1. **Type extension** (supersedes the six-field pin at Plan 04 LEARN-001 Step 1): `SessionLearningEvidence` (Plan-04-owned `learning_recap.rs`) additionally carries `#[serde(default)] current_question: Option<StudyQuestion>` (the new-turn authorization gate) and `#[serde(default)] answered_questions: Vec<StudyQuestion>` (the replay binding — a redelivery re-gates against the question it was graded by, since its own Advance moved the cursor; carrying the full question preserves the per-response changed-payload guard's width). Both default-safe under `deny_unknown_fields`; fixtures byte-unchanged; the type never crosses a wire contract (no rubric can reach the browser); the recap fold ignores both. Rejected alternative: a new session-scoped read on Plan-06-owned `ports.rs` (larger cross-plan surface).
2. **Supplementary admission**: the fix merges as a coordinator-admitted supplementary PR from the lane-04 worktree, recorded as an appendix to node 04B; Plan 15's node manifest reads as amended to include it.
3. **Plan 09 obligation** (binding, mirrored into LEARN-011's post-merge consumer verification): the memory and Postgres `session_learning_evidence` implementations MUST populate `current_question` from the persisted progression cursor (with rubric and source) and `answered_questions` with the question each persisted outcome names (one entry per distinct question_id, order unread). Dispatched as a lane-09 follow-up unit and merged inside node 09.
4. **Interim window note** (no waiver needed — no test reds): between the supplementary 04B-fix merge and node 09, real store-backed spoken answers fail closed with Unavailable (never a wrong grade); in-test fake stores populate the fields, so all suites stay green. Node 09 closes the window.

## A-15 (2026-08-25) — Node 09 admission: redaction sanction rows and the count-truth-table fixture ruling

1. **Sanction rows (A-12 mechanism):** `scripts/redaction-control.mjs` gains 31 SOURCE_AUDIT_SAFE_MARKER_OCCURRENCES rows (242 anchored literal patterns) for Plan 09's data-store, migration, and observe sources — plan-pinned field names (session_token, answer_text, prompt, authorization, pasted_text, has_word_token, …) in store/schema/test code. Any new marker occurrence in these files still fails. Plan 12 retains audit-config ownership and reconciles at 12B.
2. **Count-truth-table fixture ruling** (closes the lane's Task 1 Step 4 escalation): the store write-count truth table stays a Plan-09-owned root fixture consumed via `include_str!`; no row is added to Plan 05's `agent/fixtures/voice-protocol/v5/manifest.json` (the table is a store contract, not a wire-protocol fixture). Plan 05's manifest scope is unchanged.

## A-16 (2026-08-26) — Node 07 admission: redaction sanction rows and one probe-literal reword

1. **Sanction rows (A-12/A-15 mechanism):** 41 SOURCE_AUDIT_SAFE_MARKER_OCCURRENCES rows (92 anchored literals) for Plan 07's live-adapter sources, generated from the audit's own detection over the lane diff and keyed by BOTH the evidence marker and the structural field each line trips (the dual-branch detection previously made single-keyed rows silently insufficient). api_key/CARTESIA_API_KEY/"Bearer " rows verified line-by-line as parameter passing, validation, and header construction — never values, never logging. Plan 12 reconciles at 12B.
2. **Probe-literal reword:** the live-label-probe test used `"sk_car_live_label_probe"`, mimicking Cartesia's real secret prefix — the audit's refusal was CORRECT and the literal is reworded (`viva-not-a-real-key-label-probe`) instead of sanctioned. Secret-styled fake credentials remain forbidden even as test fixtures.

## A-17 (2026-08-26) — Node 08 admission: Plan-05-file mechanical fallout newly visible at agent-service compile restoration

Both items were structurally invisible until node 08's restoration made agent-service reach clippy/tests (the crate never compiled on any earlier tip). **Ratified, applied by the lane-08 worker as separate clearly-labeled commits citing A-17; Plan 05 retains ownership of every touched file:**

1. **protocol.rs clippy fixes (4)**: the three constant-relationship pins at ~3815-3817 keep their assert intent under `#[allow(clippy::assertions_on_constants)]` with a one-line comment naming them deliberate frame-size contract pins (do NOT delete or weaken the assertions); the ~4756 `needless_borrow` becomes `serde_json::to_value(classified)`.
2. **Frozen v4-era fixture completion**: the unversioned `agent/fixtures/voice-protocol/` study-session fixtures gain the `concept_id` (and `rubric` where the type requires it) fields Plan 04's merged `StudyQuestion` now mandates — values pinned to what the merged runtime actually emits, verified by the existing output-match tests (`*_matches_full_session_fixture_exactly`), never invented. This is completion, not retirement: Plan 05 Task 9 Step 6's v5 retirement wave remains scheduled before Plan 15 (the in-tree "frozen … retired once every consumer migrates" note stays true), and `count-truth-table.json` remains untouchable per A-15.

## W-06 (2026-08-26) — Known-red window: seven v4-fixture protocol tests until Plan 05's retirement wave

A-17's fixture completion closed the `concept_id`/`rubric` gap, but the frozen unversioned v4-era fixtures are stale in four further structural ways (no `turn_id` on question_started — supplied only through the harness, v1 recap shape without the v2 `schema`/`concepts`/`deferred_turns`/`partial` fields, the removed `{"type":"text"}` client frame kind, missing v5 `client_generation_id`/`session_token` on session_config). That is exactly Plan 05 Task 9 Step 6's retirement scope — a harness + wire-expectation migration, not field completion — and it edits the same `protocol.rs` lane 08 is actively rewriting.

**Recorded:** from now until the Plan-05 v5-retirement follow-up merges, `cargo test -p agent-service` carries exactly seven reds — parses_shared_full_session_fixture, parses_shared_fake_cartesia_gemini_session_fixture, deserializes_shared_audio_end_frame_from_full_session_fixture, serializes_audio_turn_accepted_server_frame, synthetic_runtime_output_matches_full_session_fixture_exactly, fake_cartesia_gemini_runtime_output_matches_full_session_fixture_exactly, cancelling_active_synthetic_response_suppresses_memory_writes. Node 08's merge gate and full-workspace milestone read as "green except exactly these seven names"; any other red blocks normally. **Expiry:** the retirement follow-up dispatches to Plan 05's lane immediately after node 08 merges (migrating the three fixtures plus `push_next_browser_frame`/`legacy_session_config` to v5 and retiring the frozen files per the in-tree note), and must merge before Plan 15's Level 2 full-green gate. `count-truth-table.json` survives per A-15.

## A-18 (2026-08-26) — Canonical concept identity is the unprefixed form; Plan 04's fixture anomaly corrected

Lane 08 surfaced a cross-lane fixture-identity split invisible to every package-scoped gate: `agent_domain::fixture_question()`/`fixture_rubric()` use `concept-oxidative-phosphorylation` while Plan 09's seeded store, the migrations seed, `fixture_recap()`, `packages/core`'s production seeds, the web fixtures, and the wire fixtures' own concept records all use the unprefixed `oxidative-phosphorylation` family — so every store-backed answer over the seeded fixture correctly failed closed (the service was right; the data was self-inconsistent). Repo census: the prefixed form exists in exactly two files; the unprefixed form is the ecosystem.

**Ratified (branch (a)):** the unprefixed form is canonical program-wide. `fixture_question().concept_id` and the `fixture_rubric()` criteria ids drop the `concept-` prefix (Plan-04-owned `study.rs`); the one adapter test carrying the prefixed literal follows (Plan-07-owned); the three A-17-completed v4 wire fixtures re-pin their `concept_id` values through their existing output-match arbiters. Applied by the lane-08 worker as A-18-labeled commits under coordinator authorization; owning plans retain ownership. Post-apply verification: `-p agent-domain`, `-p data`, `-p agent-adapters` all fully green plus the lane's own affected websocket tests. Branch (b) (prefixing the store side) is rejected: it would churn merged migration seeds and manufacture new mismatches against the unprefixed web/core surfaces at nodes 10/11.

## A-19 (2026-08-26) — Lane 08 dual-instance incident: commit provenance and the streamed-audio assertion adjudication

During node 08's restoration, coordinator replies to the running workflow's worker unintentionally resumed its transcript as a second concurrent instance (root cause recorded in the coordinator log; the operational rule now in force: rulings are published only in this document, never messaged to in-flight workflow agents). The duplicate instance authored four commits on `review-remediation/08-service-runtime` before being stopped: `1999061` (content = the disciplined instance's restoration, independently byte-verified by both instances; only the message's claims were inaccurate), and `045f10d`/`96282a9`/`8967581` (the duplicate's own v5 test rework). History is append-only: none of these are amended or rebased.

**Ratified dispositions, binding on the re-dispatched lane 08 worker:**
1. `1999061` stands as the restoration baseline.
2. `045f10d`, `96282a9`, `8967581` are UNADOPTED until audited: the worker examines every hunk and either adopts it under its own RED/GREEN derivation or reverts it with a forward commit. Nothing in them may be silently trusted.
3. **The streamed-audio assertion change inside `045f10d`** (AnswerEvaluated 1→0, ConceptStatus 1→0, TurnDeferred 0→1; CloseCode Policy→Protocol) is a semantic adjudication, not a mechanical hunk: the worker must determine the composed evaluator's actual honest outcome for a placeholder byte-count transcript under the merged Plan 04 rubric (a corpus-bound SyntheticFixtureAnswerEvaluator cannot semantically evaluate a placeholder — deferral may genuinely be the honest outcome per the A-08 truthfulness doctrine), derive it RED-first from a reverted baseline, and pin whichever outcome the merged system honestly produces. If the honest outcome is deferral, the worker's report MUST say so explicitly so the coordinator can annotate the CRIT-AUDIO-01 PROVEN rows (their "one evaluation per turn" proof predates the Plan 04 evaluator) and Plan 15 Level 3's browser-matrix expectations. The close-code change needs its own derivation or reversion.

## A-20 (2026-08-26) — Two lane-08 escalations: ADAPTER-06 kind-erasure routed to Plan 07; slow_client handshake fix ruled in-scope

1. **ADAPTER-06 follow-up (Plan 07, dispatched)**: `agent-adapters/src/cartesia_gemini/mod.rs:411`'s `.map_err(|_| outcome_contract_failure(...))` discards `PortErrorKind`, so a Durability store failure at voice-session open reaches operators as `tool_executor_failure` instead of `durability_degraded`. The service side is CORRECT to refuse deriving a class from anything but the typed failure (DOMAIN-009). A lane-07 follow-up preserves the port error's kind across `select_session_question` and every sibling `|_|` executor-error mapping, yielding a typed brain failure whose terminal reason is `DurabilityDegraded`. **Lane-08 admission item:** when the lane rebases onto a tip containing that fix, flip `websocket_brain_open_store_failure_reports_the_brains_typed_terminal_reason` back to `TerminalSessionReason::DurabilityDegraded` and its original name (the in-tree CHARACTERIZATION marker states this condition). No other service change.
2. **slow_client closing-handshake defect (lane 08, ruled IN SCOPE)**: `ws.rs:1800` infers write-side failure from the sanitized terminal LABEL, so a `SlowClient` terminal is excluded from `finish_closing_handshake` and a pipelining client receives `Io(ConnectionReset)` instead of its terminal frame + Close. Fix = an explicit write-failed boolean threaded through the session loop (never inferred from the label), owned by Plan 08's session-loop rewrite (T8-10 era; a dedicated unit if that has passed), derived RED-first from the worker's recorded deterministic reproduction (one unresolved answer → denied overlap → ~192KB filler). Not to be made as an unreviewed tail edit — it gets its own RED/GREEN cycle.

## A-21 (2026-08-26) — Node 08 admission: redaction sanction rows; W-06 extended by Group C

1. **Sanction rows**: 92 dual-keyed SOURCE_AUDIT_SAFE_MARKER_OCCURRENCES rows (373 anchored literals) for Plan 08's agent-service sources, generated from the audit's own detection; secret-marker hits verified as RedactedSecret handling code, never values. Plan 12 reconciles at 12B.
2. **W-06 Group C**: the two frozen-fixture replay tests (`real_websocket_replays_synthetic_fixture_and_evidence_pack`, `real_websocket_replays_fake_cartesia_gemini_fixture_and_evidence_pack`) share W-06's root cause (v4-era client frames lacking the v5 required members) and join its known-red set — nine names total, same expiry: the Plan 05 retirement follow-up, before Plan 15 Level 2.
3. **A-20.1 flip-back applied** at this admission (c93ab80 + b97880c): the open-store-failure test expects `DurabilityDegraded` on both the wire and the evidence detail, its defect-characterization marker replaced by the closed-state record.

## Coordinator annotation (2026-08-26) — CRIT-AUDIO-01 row 298 and Plan 15 Level 3 expectations

Ledger row 298's proof cell ("successful transcript/evaluation/recap") was PROVEN at the node 03 merge under the pre-Plan-04 grading path. Node 08's RED-first adjudication (A-19.3) established that on the merged system the same streamed-audio path now honestly yields transcript → `TurnDeferred{InsufficientSemanticEvidence}` with zero mastery writes — the corpus-bound fixture evaluator is response-id-indexed and transcript-blind, and a mutation control proved no route manufactures a graded outcome for a placeholder transcript. Row 298's PROVEN status stands for what it proved when proved (bounded frames, end-turn, backpressure, cancel, transcript); its evaluation/recap half is superseded by the Plan-04 evaluator design. **Plan 15's Level 3 browser-matrix expectations must assert deferral, not evaluation, on the placeholder-transcript path**; a real graded flow is exercised through the fixture-known-answer paths.

## A-22 (2026-08-26) — The turn-outcome authority must satisfy the browser-event authorization gate; W-06 narrowed to Group C

Plan 05's retirement work (node-05 appendix, PR #116) diagnosed why the two Group C replays stay red: `authorize_answer_evaluation` (data/memory/authorization.rs) admits a browser `answer_evaluation` event only when the persisted `answer_attempts` row carries the matching evaluation payload AND an `event_authorizations` digest — and the only writer of both was `record_answer_evaluation`, which Plan 04's turn-outcome authority retired. The production path (`evaluate_spoken_answer` → `record_turn_outcome`) populates neither, so every genuinely evaluated turn is refused at the socket (VOICE_CLIENT_AUTHORITY_FORBIDDEN) — a successful transcript → evaluation over a real socket is currently unreachable, bearing directly on Plan 15 Levels 2/3.

**Ratified (A-14 pattern):** Plan 09's memory and Postgres `record_turn_outcome` implementations MUST, for an outcome whose resolution is Evaluated, atomically populate the attempt row's evaluation payload and write the event-authorization digest exactly as the retired writer did — the browser-event gate's invariants transfer to the turn-outcome authority unchanged (no gate weakening; Plan 08's socket gate is correct as-is). Conformance tests in the shared suite prove both backends; the lane-05 evidence-pack behavioral expectations (store_snapshot/usage/evidence_events/terminal_close_reason) are then verified and pinned as the Group C flip-back. **W-06 now reads:** seven names CLOSED at PR #116; the known-red set is exactly the two Group C replays, expiring at this amendment's fix + flip-back, before Plan 15 Level 2.

## A-23 (2026-08-26) — Node 11 admission ratifications

1. **Sanction rows** (A-12 mechanism): 7 dual-keyed rows / 18 anchored literals for Plan 11's `shared.ts` and library route — matching the lane's line-by-line verified request exactly; the lane never edited the audit config nor renamed anything to dodge it.
2. **LandingEntry.test.tsx cross-lane edit ratified** (A-08 precedent): three mechanically-forced lines (save/set/restore `VIVA_WEB_CANONICAL_ORIGIN`) required by Plan 11's own WEBAPI-003 canonical-origin enforcement — reviewer-verified as forced and assertion-preserving; Plan 13 retains ownership.
3. **Plan 11 Task 2 Step 1's rejection union reads as amended to eleven values**: `malformed_shape` joins, forced by Plan 05's read-only vectors (VOICE-TOKEN-REJECT-SEGMENT-SHAPE / WRONG-PREFIX both pin it, and Plan 11's own precedence rule says the exact Plan 05 vector rejection wins). The fixture is untouched.
4. **Routing confirmations**: `VIVA_SESSION_SECURITY_STORE_REST_TOKEN` validation lands with Task 4 (obligation recorded in-tree on validatedSecret(); the Task 4 dispatch carries it); the paste/file/retry POST origin-authorization carve-out is Task 5's scope per the plan's own ingestion-contract text. The `packages/core` single red the lane reported is the recorded Phase-14B boundary test, not new.

## A-24 (2026-08-26) — A-22 adjudications: the digest's true shape, the concept-status sibling, and the forced flips

The A-22 unit's adversarial reviewer correctly refused to let a lane decide unilaterally what an authorization gate stops binding. Coordinator adjudication of all its findings:

1. **Digest shape ratified**: the shared browser-event digest is `AnswerEvaluationEventPayload` — `AnswerEvaluation` minus `answer_text` — used symmetrically by the gate and every writer on both backends. A-22's "exactly as the retired writer did" is unsatisfiable as written: `TurnOutcome` carries no transcript, the store publishes `transcript_persistence: false` by privacy design, and the envelope's keyed commitment is `None` in both production adapters with no store-held key. **Recorded exposure, accepted**: a provider event pairing a genuinely graded `response_id` with fabricated `answer_text` is admitted where the retired path refused it — marginal risk ≈ zero because the identical transcript surface already reaches the same browser ungated via `transcript_delta`/`transcript_final` (preflight's catch-all), and every learner FACT (grade, status, schedule) remains fully bound. If a persisted transcript commitment ever lands (new Plan-04/07 work), the gate re-tightens then.
2. **Concept-status sibling ratified**: A-22's obligation extends to the session-scoped `concept_statuses` row — `authorize_concept_status`'s only writer was the same retired path, and the program's own frozen v5 evidence packs pin `concept_statuses = 1` for a graded turn. Both backends write it inside the same transaction.
3. **Forced cross-lane flips sanctioned** (A-18 category, sanctioned at adjudication rather than in advance — the lane labelled and disclosed each hunk with revert recipes): the four `concept_statuses` assertions in `agent-service/src/protocol.rs` (Plan 05/08) and `agent-adapters/tests/cartesia_gemini.rs` (Plan 07) flip empty→1; both frozen packs already read 1 at LANE_BASE, so reverting installs reds against the program's own contract. Owners retain their files.
4. **Evidence-pack re-pins accepted and named**: ordering (SourceReference before AnswerEvaluated, matching the documented projection) and the four `-generation-1` detail suffixes — derivations independently verified by the reviewer.
5. **Follow-ups recorded**: (a) memory-backend two-phase apply (compute every decision/digest, then mutate) to remove the residual partial-commit class on multi-concept outcomes — a lane-09 micro-unit before Plan 15; (b) `evaluation_label_wire` duplication (data + adapters) — Plan 04 hoist candidate beside `is_known_evaluation_label`, recorded for the Plan 15 completeness sweep.

## A-25 (2026-08-26) — Node 11 admission: sanction rows and the node-10 client obligations

1. **Sanction rows**: 11 dual-keyed rows for Plan 11's Tasks 4–11 surfaces — header construction, type members, prefix-REFUSAL literals, parse regexes, URL-credential refusal checks. Plan 12 reconciles at 12B.
2. **W-05 re-enumeration at the node 11 merge**: the window's membership is unchanged (3 Plan-10 files) but the counts have healed to 13 test failures / 20 typecheck errors (the display-state failures were closed by A-08). Node 10 remains the expiry.
3. **Node-10 client obligations recorded** (join the accumulated list): send `{refresh_token, session_id, study_set_id, user_id}` to the refresh endpoint and parse `refresh_expires_at`/`session_absolute_expires_at` as RFC3339; preserve `Sec-Fetch-Site` on projection calls (same-origin GETs carry no Origin header — fetch metadata is the only proof); strip the `user_id`/`session_id` members from ingestion POST bodies (`deny_unknown_fields` now rejects them); remove `initial_goal` from session_config; retire the root `server-ready.json` fixture residue.

## A-26 (2026-08-27) — Node 10 T11-13 adjudications

1. **page.tsx hunks sanctioned** (A-18-category forced fallout, reviewer-verified both halves): Task 12's D-06B deletion of `vivaStaticExportEnabled` forced dropping its only import in Plan-13-owned `apps/web/app/page.tsx` (keeping it fails the lane's own typecheck gate; re-exporting fails the deletion grep). Behavior is preserved-or-stricter (strictly more redaction). **Consequence recorded for 13B**: Plan 13's Branch-B step "remove vivaStaticExportEnabled composition from app/page.tsx" is already discharged — 13B executes it as a VERIFICATION (grep-absence), not an edit; its RED is pre-empted by design, not skipped.
2. **`voiceTraceLabelPlanComputations` interface addition ratified**: a test-only observable counter (module-level, monotonic) whose removal would delete the only mounted proof that per-frame replanning does not happen. Accepted with the impurity noted; Plan 15's completeness sweep may revisit a cleaner pattern.
3. **`suppliedIngestionValue` ratified**: correct-behavior scope addition (whitespace-only exam date reads as unset), triple-pinned with an over-reach guard.
4. **T14-CLOSE items** (lane-10-owned fixes riding the final unit): (a) drop `node.status` from `conceptSetSignature` — no label-plan input depends on it and every status flip currently triggers a full rebuild with a visible particle pop; pin with a test on the computations observable; (b) the forged-tool-result and oversized-frame tests must assert the fixture's own `diagnostic_code`/`path` fields, not local literals (the cross-language coupling is the fixture's purpose). Task 11's structural-only RED is recorded as an evidence-quality note (the behavioral claim stands via mutation M1).

## A-27 (2026-08-27) — Node 10 closing adjudications

1. **Sanction rows**: coordinator applies rows for the 44 verified marker lines across the lane's three client files (type members, wire field names, protocol discriminants — `answer_text` is Plan 05's own turn-intent literal — header construction, doc prose). Plan 12 reconciles at 12B.
2. **WEBSESSION-DEFERRED-01 surface ruling** (lane-escalated: deferrals are silent on screen — the projection authors pinned copy but nothing renders it, and a retry BUTTON would be a D-02 progression decision the browser must not make): ratified as a **display-only deferral surface** — render the projection's existing `deferredTurnOutcome` copy (reason text + "Retry this question"/"Wait for the next question" guidance) in the session UI, with NO client-side retry action: answering again is the retry, and the server's progression cursor governs which question an answer binds to (A-14 semantics). One lane-10 follow-up unit, RED-first at the mounted level; merges inside node 10.
3. **Routed to 13B's dispatch**: the two pre-existing accessibility-harness reds the sweep surfaced (`--owned-surfaces` Playwright/CSP harness throw; `--session-handoff --disclosure-scope all-live-content`, which the script's own header names a Plan 13B deliverable).

## A-28 (2026-08-27) — Node-10 mapping rulings and the 13B vault-swap obligation

1. **Row 319 ruled flipped**: the finding was cross-surface interval CONFLICT; with one persisted authority and zero client fabrication, the verdict rendering the status label alone is the honest settled state (the recap surface carries the only interval, from the persisted decision). The stale "interim bridge / node-10 rewrite" comment is corrected in the same commit; the coordinator's PR #119 description overstated this deliverable and the log records the correction.
2. **Rows 224/561/612 ruled flipped**: the ADAPTER-02 client staleness-guard half is satisfied by the pre-existing second-turn test (viva-agent-client.test.ts) plus node 10's mounted multi-turn matrix, verified on the merged tip rather than program-authored RED — recorded for Plan 15's independent review.
3. **Row 598 held** for 13B's browser-story gate (the Frontend C acceptance block closes on the combined-tree Playwright stories); row 309 likewise (overlap/jitter clause = 13B Task 10 + frontend-performance.mjs).
4. **13B OBLIGATION (binding)**: swap `LibraryStatusPanel.tsx`'s default from the inert `pendingBrowserSessionCredentialVault` stub to Plan 10's now-published real vault export (`use-viva-agent-session.ts`), updating the two doc comments that still say "until Plan 10's real export lands" — the FRONTEND-011 vault call is otherwise a live no-op on the tip. Verified at the 13B admission gate regardless.

## A-29 (2026-08-27) — Node 13B admission: e2e selector fix, vault-liveness ruling, stub disposition

1. **e2e-browser.mjs selector fix** (A-03 category, coordinator-applied on Plan-12's file): the harness waits for the "Review missed concepts" suggestion chip that 13A's D-03B correctly deleted; the wait targets the one honest affordance ("Begin oral exam"). Plan 12 reconciles at 12B.
2. **Vault-liveness ruling** (closes the VAULT-SWAP review's open architectural question): the credential handed to the real vault does not survive the landing flow's hard navigation, because Plan 10's vault is deliberately memory-only (its header rules out URL/cookie/storage persistence as a security invariant) and the session page's mount then resolves a nonrenewable direct entry from the fragment. **Recorded as the designed state for this cycle**: landing-entered sessions run on the session token's absolute lifetime; in-session rotation works within a document; cross-document renewal would require new design (client-side route transition, or a session-token→refresh-credential exchange endpoint — Plans 10/11 territory) that NO corpus row demands. Explicitly out of remediation scope; carried as a post-program design item and named in the Plan 15 handoff.
3. **`pendingBrowserSessionCredentialVault` disposition** (the review's finding 3): RETAINED as the typed inert fallback for SSR/non-interactive contexts, with its inertness test — it documents the no-op contract; not dead code.

## A-30 + W-07 (2026-08-27) — /session dynamic rendering reapplied; the browser-harness window

**A-30**: commit f9cc747 ("opt /session into dynamic rendering so its CSP nonce applies") is REAPPLIED under coordinator ratification at the 13B admission. Its diagnosis was verified complete — without a `dynamic` export, Next's automatic CSP-nonce propagation never engages for /session, leaving every script tag nonce-less under `next start`: the live session route was unusable in ANY real deployment. The 3790ded revert was ownership discipline, not a merits rejection; the merits are now ruled. The route-segment flag changes no session semantics.

**W-07 (known-red window)**: `bun run e2e:browser` does not pass on this tree. With A-29.1's selector fix the harness reaches the session boundary, but its NON-hosted entry branch still encodes the pre-D-03B landing flow (plain button click with old auto-start semantics — zero start POSTs reach the web tier; the agent never sees a session), while its own hostedMode branch already uses the merged signed-start path. Separately, `style-src 'self'` blocks Next's inline styles in the harness run (console noise today; a real posture question for the production CSP that row 447's hold anticipated). **Owner: Plan 12B** — Task 13's harness rework against the merged D-03B/D-07A landing contract and the strict validator, plus the style-src disposition (nonce/hash the framework styles or an adjudicated relaxation with Plan 11). Expiry: before Plan 15 Level 3, which requires the real browser proof. Until then, the browser-harness red does not block admissions; every OTHER gate (mounted a11y full mode, performance sampling, unit/typecheck/build) blocks normally.

## A-31 (2026-08-27) — Node-13B mapping notes and the frontend loose-ends unit

1. **Perf evidence refreshed**: the lane's sampling predated A-30's /session rendering change; the coordinator re-ran `frontend-performance.mjs` on the merged tip (post-A-30, post-vault-swap) — green, 0 issues — so rows 309/587/595 rest on current evidence.
2. **Harness gate-wiring is NOT yet real**: no root script and no CI step invokes the frontend accessibility/performance harnesses, and `frontend-accessibility.mjs`'s in-source claim that `bun run validate` and hosted CI run it is FALSE at HEAD. **12B obligation**: wire both harnesses into validate/CI per Plan 13's L946 handoff (or correct the comment and record the hosted gate at Plan 15) — Plan 15 must not read the comment as an existing gate.
3. **D-08 substitute-path caveat recorded**: rows 307/494/656 flip on the plan's named substitute (Plan 10's providerInputAllowed mutation evidence) — not a live-provider mounted assertion, which the no-secret harness structurally cannot produce.
4. **LOOSE-ENDS unit (lane 13, before Plan 15)**: (a) behavioral background-pause assertion (drive document.hidden, assert both canvases' loops stop — rows 463/558's missing clause); (b) agent-connected session zoom/320px story (rows 592/596/530); (c) keyboard disclosure traversal on /session (row 593); (d) forced-colors controls/meaningful-state sweep (row 594). Until it lands those six rows stay.

## A-32 (2026-08-27) — The signed-start → projection → socket deadlock; adjudication and owners

Lane 12's honest browser gate proved by direct probe that a newly started session can NEVER obtain its authenticated projection on the merged tree: the start mints a session id but writes no `voice_sessions` row; `authenticated_study_projection` requires one (both backends); the row's only writer is the socket's own provisioning; and the client opens no socket until the projection validates (`connectionEligible`). Circular wait — masked historically by the retired trusted-loopback entry's pre-seeded row. Owners: Plan 08 (session lifecycle), with Plan 04/09/10 contracts correct as written.

**Ratified fix (restores the retired path's semantics at the right layer):** the agent-service signed-start/mint path atomically records the voice session (via the existing `record_voice_session` port; both backends implement it) with the same identity the socket later claims — a started session IS a session. The socket's own provisioning becomes idempotent-on-existing (the StudyStoreWriteOutcome vocabulary already models it). The projection then validates pre-socket, and `connectionEligible` proceeds. Explicitly REJECTED: weakening the projection's row requirement, and reordering the client's trust flow. Also in scope for the same lane-08 unit: the fixture-seeding development command (SERVICE-013 places fixture setup "in tests and development commands"; none exists — the CI browser job and local gates need it; `data::seed_postgres_fixture` is public and caller-less).

**Style-src disposition (W-07's second half, per lane 12's measurement):** all 35 violations are Next framework-injected `<style>` elements. Ratified: Plan 11 propagates the existing CSP nonce to `style-src` in `apps/web/proxy.ts` (A-30's dynamic rendering makes the nonce engage) — no `'unsafe-inline'`.

## A-33 (2026-08-27) — The Cargo.lock pruning premise is false; the RSA advisory gets a scoped, evidence-cited ignore

Lane 12b-a proved with a minimal-crate experiment that Plan 12 Task 14's "prune `rsa`/`sqlx-mysql`/`sqlx-sqlite` from `agent/Cargo.lock`" is unachievable: Cargo.lock is feature-agnostic and records every optional dependency regardless of activation. The actual security property IS achieved and permanently gated: `cargo tree -i` proves none of those packages exist in any compiled target, asserted by the new dependency-policy suite. The residual is exactly one advisory — RUSTSEC-2023-0071 (`rsa` 0.9.10, no fixed upgrade exists) — reachable only through the never-compiled MySQL driver.

**Ratified:** the plan's "do not add an ignore for the unfixed RSA advisory" is superseded (it presumed the impossible pruning): `cargo audit` carries a scoped ignore of exactly `RUSTSEC-2023-0071`, co-located with a comment citing the build-graph absence proof and the dependency-policy gate that keeps it honest — any change that makes the RSA path compilable fails the policy suite before the ignore could hide anything. A permanently red audit gate was rejected (normalizes red). **Recorded post-program closure:** the workspace `sqlx` 0.9 upgrade (probe-confirmed to drop `rsa` entirely) removes the ignore — semver-major, deliberately not taken at the program's tail; Plan 14's missing `default-features = false` obligation (B-2) rides with it. Also recorded: the ratchet froze the honest at-SHA measurements (memory 6314 / postgres 2005 — the dispatch's cited numbers were lane-09's pre-appendix figures), and the e2e extraction debt (D-1, `RELEASE-030-E2E-EXTRACTION`) is assigned to the HARNESS-W07 unit, which reworks that file anyway. Operational note: bun 1.3.3 lacks `jest.advanceTimersByTime` — test suites run on ambient bun; 1.3.3 is for lockfile work only.

## A-34 (2026-08-27) — SESSION-AT-START adjudications: the four review findings

1. **Cross-lane BFF hunks RATIFIED** (A-18 category): the web mint path (Plan-11-owned `shared.ts`: mintSessionFromLibrary + agentLibraryUrl, plus its tests) presents the session-mint authority — content determined by the ratified record-at-mint mechanism; the reviewer confirmed the engineering. Plan 11 retains ownership.
2. **Completion ORDERED — the fix must work on non-loopback binds**: the library route's bearer check 401s the mint before the A-32 gate on any public bind, while the config REQUIRES the mint credential exactly there. The route must accept the session-mint authority as a valid credential FOR THE MINT/RECORD OPERATION on non-loopback binds (scoped — it grants no library-read authority), proven in the public-bind test shape (`with_unauthenticated_paste_allowed(false)` + required bearers set), RED-first from the reviewer's exact scenario.
3. **Redaction-dodge REVERSED**: the mint credential's parse site returns to the sibling `RedactedSecret::from` spelling and the grouped re-export; the coordinator supplies the sanctioned row (the A-12 mechanism exists for exactly this). The norm stands: never spell around the audit — request the row.
4. **The new configuration key RATIFIED, contingent on item 2**: `VIVA_AGENT_SESSION_MINT_BEARER_TOKEN` (32–512 bounds, collision set, session-token-secret pairing, required on public binds) joins Plan 08's fixed-interface table as amended. Downstream obligations: the 12B CI browser job's environment and a `docs/deployment-runbook.md` session-mint section (Plan-12-owned; rides the combined 12B admission via the runbook harness 12b-c built).
